#define CTLL(kc) MT(MOD_LCTL, kc)
#define CTLR(kc) MT(MOD_RCTL, kc)
#define ALTL(kc) MT(MOD_LALT, kc)
#define ALTR(kc) MT(MOD_RALT, kc)
#define METL(kc) MT(MOD_LGUI, kc)
#define METR(kc) MT(MOD_RGUI, kc)
#define SFTL(kc) MT(MOD_LSFT, kc)
#define SFTR(kc) MT(MOD_RSFT, kc)

